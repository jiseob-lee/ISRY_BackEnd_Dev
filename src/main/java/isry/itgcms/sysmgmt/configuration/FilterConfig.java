package isry.itgcms.sysmgmt.configuration;

//import org.springframework.beans.factory.annotation.Autowired;
//import org.springframework.context.annotation.Bean;
//import org.springframework.context.annotation.Configuration;
//import org.springframework.boot.web.servlet.FilterRegistrationBean;

//import isry.itgcms.sysmgmt.personalinfo.service.PersonalInfoService;
//import isry.itgcms.sysmgmt.util.SessionTimeoutCookieFilter;

//@Configuration
public class FilterConfig {
	
	/*
    @Autowired
    private PersonalInfoService personalInfoService;

    @Bean
    public FilterRegistrationBean<SessionTimeoutCookieFilter> sessionTimeoutCookieFilter() {
        FilterRegistrationBean<SessionTimeoutCookieFilter> registration = new FilterRegistrationBean<>();
        registration.setFilter(new SessionTimeoutCookieFilter(personalInfoService));
        registration.addUrlPatterns("/*");
        return registration;
    }
    */
}
