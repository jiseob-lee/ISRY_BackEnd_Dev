<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:context="http://www.springframework.org/schema/context"
    xmlns:task="http://www.springframework.org/schema/task"
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
                        http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-4.0.xsd
                        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd">
    
    <!-- ===================================================== -->
    <!--                task / executor / scheduler  설정             -->
    <!-- ===================================================== -->                    
    <!--
    <task:annotation-driven executor="executor" scheduler="scheduler" proxy-target-class="false"/>

    <task:executor id="executor" keep-alive="10" pool-size="10-20"
        queue-capacity="10" rejection-policy="ABORT" />
        
    <task:scheduler id="scheduler" pool-size="4" />
    -->

    <!-- ===================================================== -->
    <!--                task:scheduled-tasks 설정                            -->
    <!-- ===================================================== -->
    <!--
    <task:scheduled-tasks scheduler="scheduler">
        <task:scheduled ref="logCollector" method="collect" cron="*/5 * * * * *" />
    </task:scheduled-tasks>-->
   
    <!-- ===================================================== -->
    <!--                sample Test                            -->
    <!-- ===================================================== -->
    <!--
    <bean id="logProcessor" class="egovframework.com.cmm.task.LogProcessor" />
    
    <bean id="logCollector" class="egovframework.com.cmm.task.LogCollector" />-->

</beans>
