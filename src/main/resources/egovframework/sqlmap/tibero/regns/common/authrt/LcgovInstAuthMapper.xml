<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="isry.regns.common.authrt.mapper.LcgovInstAuthMapper">

    <select id="selectLcgovInstList" resultType="Integer">
    <![CDATA[
        /* selectLcgovInstList - Lee MS */

            SELECT
                UP_INST_NO             /* 상위기관번호 */
            FROM SAA000     /** 통합기관 */
            WHERE INST_NO = #{INST_NO}
    ]]>
    
    </select>


</mapper>