<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="isry.couns.stats.dscsnstats.mapper.EumSatiStatsMapper">

    <select id="selecteumSatiStats" parameterType="HashMap" resultType="HashMap">        

			SELECT  /*SQL-ID :iisry.couns.stats.dscsnstats.mapper.EumSatiStatsMapper."selecteumSatiStats" : 이음-e 만족도 통계*/
			       ATFINO                                    AS  IMAGEID
			      
			FROM    SBA110 
            WHERE   FRST_REG_DT    BETWEEN   TO_DATE(#{STARTDATE}, 'YYYYMMDD')
                                    AND      TO_DATE(#{ENDDATE},   'YYYYMMDD') + 0.99999
 			AND     ATFINO         IS  NOT  NULL 
			AND     DEL_YN         =  'N'     
			GROUP   BY  ATFINO
			ORDER   BY  LENGTH(ATFINO), ATFINO 
   </select>
 
    

</mapper>