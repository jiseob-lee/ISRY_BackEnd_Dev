<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="isry.itgcms.sysmgmt.userauth.mapper.InqOrgDtlListMapper">

    <select id="selectOrg" resultType="HashMap">
    <![CDATA[
        /* selectOrg - Lee JiSeob */
        
        SELECT
            INST_NO,
            INST_NM,
            UP_INST_NO,
            DEL_YN,
            FRST_RGTR_ID,
            TO_CHAR(FRST_REG_DT, 'YYYY-MM-DD HH24:MI') AS FRST_REG_DT,
            LAST_MDFR_ID,
            TO_CHAR(LAST_MDFCN_DT, 'YYYY-MM-DD HH24:MI') AS LAST_MDFCN_DT,
            SRTNG_SQNCE
        FROM SAA000 /* 통합기관 */
        ORDER BY INST_NM, INST_NO
        
    ]]>
    </select>
        
</mapper>
