<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="isry.couns.stats.etcstats.mapper.ConnGraphStatsMapper">

    <select id="selectconnGraphStats" parameterType="HashMap" resultType="HashMap">        

			SELECT  /*SQL-ID : isry.couns.stats.etcstats.mapper.ConnGraphStatsMapper.selectconnGraphStats : 접속통계 그래프 */
			       ATFINO                                    AS  IMAGEID
			      
			FROM    SBA110 
            WHERE   FRST_REG_DT    BETWEEN   TO_DATE(#{STARTDATE}, 'YYYYMMDD')
                                    AND      TO_DATE(#{ENDDATE},   'YYYYMMDD') + 0.99999
 			AND     ATFINO         IS  NOT  NULL 
			AND     DEL_YN         =  'N'     
			GROUP   BY  ATFINO
			ORDER   BY  LENGTH(ATFINO), ATFINO 
   </select>
 
    

</mapper>